<p>
  <h1>Perfil:
    <%= @user.name %>
  </h1>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to t('links.edit'), edit_user_path(@user) %> |
<%= link_to t('links.back'), users_path %>
