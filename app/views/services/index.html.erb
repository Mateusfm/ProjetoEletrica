    <% @services.each do |service| %>
    <div class="feed-body">
            <div class="feed-img">
              <%= link_to (image_tag (service.imagem)), service%>
            </div>

      <div class="feed-text">

        	<h1>
         	  <%= service.title %>
        	</h1>

        	<p>
         	  <%= service.description %>
        	</p>

      </div>

      <div style ="width: 190px; float: right;">
        <%= link_to 'Pedir orçamento', new_contact_path %>
      </div>

    </div>

    <% end %>
