 <%= render 'form_search'%>

   <% if user_signed_in? %>

    <% if policy(@produtos).index? %>

      <%= render 'admin_index' %>

    <% else %>

      <%= render 'visitor_index' %>

    <% end %>
  
  <% else %>
   
   <%= render 'visitor_index' %>
  
  <% end %>
